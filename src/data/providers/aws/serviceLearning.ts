import type { ServiceLearningProfile } from "../../../domain/types";

export const awsServiceLearning: Record<string, ServiceLearningProfile> = {
  "aws-route-53": {
    summary: [
      "Route 53 is AWS's highly available authoritative DNS service. It translates names into endpoints and can make routing decisions based on health, geography, latency, or weights.",
      "In SAA designs, Route 53 is commonly the first decision point for public traffic and failover, but it does not proxy application requests or replace a load balancer.",
    ],
    features: ["Alias records for AWS endpoints", "Health checks and DNS failover", "Simple, weighted, latency, geolocation, and multivalue routing", "Private hosted zones for VPC name resolution"],
    useCases: ["Route users to CloudFront or an ALB", "Active-passive regional failover", "Blue/green or weighted migrations", "Private service discovery inside a VPC"],
    limitations: ["DNS caching means changes are not always immediate", "Health checks do not inspect application behavior as deeply as an ALB target health check", "It does not provide TLS termination, request filtering, or HTTP routing"],
    configuration: ["Choose a hosted zone scope: public or VPC-associated private zone", "Use alias records for AWS resources where supported", "Set TTLs to balance failover speed with resolver and cost efficiency", "Pair health checks with failover or latency policies when resilience is required"],
    advancedConfiguration: ["Use DNSSEC for signed public zones", "Use traffic flow for complex policy composition", "Plan resolver endpoints and forwarding rules for hybrid DNS", "Document the failure mode when every health check becomes unhealthy"],
    connectionNotes: { "aws-cloudfront": "Resolves users to the CloudFront distribution.", "aws-alb": "Routes DNS names to a regional load balancer.", "aws-s3": "Can point a website name at an S3 website endpoint." },
  },
  "aws-cloudfront": {
    summary: ["CloudFront is a global CDN that caches and delivers content from edge locations close to viewers.", "It can also accelerate dynamic APIs, terminate TLS, apply edge security, and shield origins from repeated requests."],
    features: ["Cache behaviors by path", "Origin access control for private S3 origins", "TLS certificates and custom domains", "Signed URLs and cookies", "CloudFront Functions and Lambda@Edge"],
    useCases: ["Static websites and media", "Global API acceleration", "Private S3 content delivery", "Edge redirects, headers, and lightweight personalization"],
    limitations: ["Cached content can be stale until expiration or invalidation", "It is not a general-purpose compute layer", "Some request and response behavior differs between Functions and Lambda@Edge", "Origin latency still matters for cache misses"],
    configuration: ["Define an origin and one or more path behaviors", "Choose cache key, allowed methods, compression, and protocol policy", "Use Origin Access Control instead of public S3 buckets", "Configure access logs, WAF, and an ACM certificate in us-east-1 for the distribution"],
    advancedConfiguration: ["Use origin groups for failover", "Tune cache policies separately from origin request policies", "Design invalidation and versioned asset strategies", "Use response headers policies and field-level encryption where required"],
    connectionNotes: { "aws-s3": "Caches static objects and protects the bucket as the origin.", "aws-alb": "Accelerates dynamic regional web traffic and adds edge TLS/security.", "aws-waf": "Filters viewer requests before they reach the origin." },
  },
  "aws-api-gateway": {
    summary: ["API Gateway is a managed entry point for REST, HTTP, and WebSocket APIs. It handles request routing and integrates with Lambda, HTTP backends, and AWS services.", "It is useful when the API itself needs managed authentication, throttling, stages, usage plans, or request transformation."],
    features: ["REST, HTTP, and WebSocket APIs", "Authorizers and API keys", "Stages, deployments, throttling, and usage plans", "Request validation, mapping, and access logs", "Private APIs through VPC endpoints"],
    useCases: ["Serverless web and mobile backends", "Partner APIs with quotas", "Synchronous request/response workflows", "WebSocket notification and collaboration APIs"],
    limitations: ["Payload, timeout, quota, and throughput limits vary by API type", "It is not a replacement for a general TCP load balancer", "Long-running work should be decoupled instead of held in one request", "Poorly designed cache keys or throttles can create unexpected behavior"],
    configuration: ["Select the API type based on protocol and feature needs", "Define resources/routes, integrations, stages, and deployment lifecycle", "Configure authentication, throttling, validation, CORS, and structured logs", "Use idempotency and correlation IDs for retryable requests"],
    advancedConfiguration: ["Use usage plans and client quotas for multi-tenant APIs", "Use VPC links for private HTTP integrations", "Model canary releases, custom domains, and mutual TLS", "Use response caching only when consistency and invalidation are understood"],
    connectionNotes: { "aws-lambda": "Invokes Lambda synchronously for request handling.", "aws-sqs": "Places work on a queue so the API can return without waiting.", "aws-dynamodb": "Can integrate with DynamoDB for simple service-backed operations.", "aws-cloudwatch": "Publishes metrics and logs for operational checks." },
  },
  "aws-s3": {
    summary: ["S3 is durable object storage organized into buckets and objects. It is the default SAA choice for large-scale files, backups, static assets, logs, and data-lake data.", "Objects are addressed by key and are not edited like rows in a database or mounted like a normal filesystem."],
    features: ["Versioning and object lock", "Storage classes and lifecycle transitions", "Server-side encryption and bucket policies", "Event notifications and replication", "Multipart upload and range reads"],
    useCases: ["Static website assets", "Backups and disaster recovery", "CloudTrail and application log retention", "Data lake landing zone", "Event-driven file processing"],
    limitations: ["S3 is object storage, not a block device or relational database", "Cross-region replication and retrieval can add cost", "Public access is easy to misconfigure", "Listing and prefix design matter for operational tooling and analytics"],
    configuration: ["Block public access unless public delivery is deliberate", "Enable versioning for recovery and replication workflows", "Select encryption, lifecycle, retention, and access logging policies", "Use least-privilege bucket and identity policies with explicit conditions"],
    advancedConfiguration: ["Use Object Lock for WORM retention", "Use replication rules with ownership and encryption planning", "Partition data by prefixes for analytics and lifecycle operations", "Use S3 Access Points, VPC endpoints, and access grants for larger environments"],
    connectionNotes: { "aws-cloudfront": "Serves objects through a global cache and private origin access.", "aws-lambda": "Triggers processing when objects are created or changed.", "aws-cloudtrail": "Stores account activity logs for durable audit retention." },
  },
  "aws-vpc": {
    summary: ["A VPC is a logically isolated regional network where subnets, route tables, gateways, endpoints, and security controls define reachability.", "A VPC is regional; subnets belong to one Availability Zone, so resilient designs normally distribute important tiers across multiple AZs."],
    features: ["IPv4 and optional IPv6 CIDR blocks", "Public and private subnets", "Route tables and network ACLs", "Internet, NAT, transit, VPN, and peering connectivity", "VPC endpoints and flow logs"],
    useCases: ["Isolated application environments", "Multi-tier web applications", "Hybrid connectivity", "Multi-VPC shared-services and inspection architectures"],
    limitations: ["CIDR ranges cannot overlap when networks must communicate", "A VPC does not automatically make resources private or highly available", "Routing, security groups, NACLs, and endpoint policies all affect reachability", "Some AWS services remain regional or AZ-scoped despite VPC placement"],
    configuration: ["Reserve non-overlapping CIDR space for growth and connectivity", "Create subnets per AZ and tier", "Associate each subnet with an intentional route table", "Add gateways, endpoints, flow logs, and DNS settings based on traffic requirements"],
    advancedConfiguration: ["Plan IPv6 egress and egress-only internet gateways", "Use centralized inspection with Transit Gateway", "Model shared VPC or RAM ownership boundaries", "Document ingress, egress, and east-west paths separately"],
    connectionNotes: { "aws-public-subnet": "Contains resources whose route table can reach an internet gateway.", "aws-private-subnet": "Contains resources without direct inbound internet routing.", "aws-transit-gateway": "Connects the VPC to other VPCs or hybrid attachments." },
  },
  "aws-alb": {
    summary: ["An Application Load Balancer distributes HTTP and HTTPS requests across healthy targets and makes Layer 7 routing decisions.", "For SAA, an ALB normally spans at least two AZs and points to target groups rather than directly representing a single server."],
    features: ["Listener rules and path/host routing", "Target groups and health checks", "TLS termination", "WebSocket and HTTP/2 support", "Integration with WAF and Auto Scaling"],
    useCases: ["Highly available web front ends", "Blue/green and canary routing", "Microservice path routing", "TLS termination in public subnets"],
    limitations: ["ALB is Layer 7 and does not handle arbitrary TCP/UDP traffic", "Targets must pass health checks before receiving traffic", "It does not replace application authentication or authorization", "Cross-zone and connection behavior affect cost and design"],
    configuration: ["Place the ALB in at least two appropriate subnets/AZs", "Define listeners, certificates, rules, target groups, and health checks together", "Allow the ALB security group to reach target security groups on the application port", "Use deletion protection and access logging for production workloads"],
    advancedConfiguration: ["Use weighted target groups for progressive delivery", "Tune deregistration delay and slow start for deployments", "Use fixed responses, redirects, authentication actions, and mTLS where appropriate", "Pair with Route 53 and WAF for resilient public ingress"],
    connectionNotes: { "aws-auto-scaling": "Registers and deregisters instances as capacity changes.", "aws-ec2": "Routes requests to healthy instances in target groups.", "aws-waf": "Filters HTTP requests before listener rules run." },
  },
  "aws-auto-scaling": {
    summary: ["An EC2 Auto Scaling group maintains a fleet within desired, minimum, and maximum capacity and replaces unhealthy instances.", "The group is a capacity controller; the instances, launch template, subnets, and load balancer targets complete the application design."],
    features: ["Desired/min/max capacity", "Launch templates and mixed instance policies", "Health checks and replacement", "Target tracking, step, scheduled, and predictive scaling", "Multi-AZ distribution"],
    useCases: ["Stateless web and API fleets", "Bursting workloads", "Self-healing compute", "Spot and On-Demand capacity mixes"],
    limitations: ["Scaling is not instantaneous and needs warm-up time", "Stateful local data is lost when instances are replaced", "Bad metrics or cooldown settings can cause oscillation", "The group cannot make a single-instance design highly available by itself"],
    configuration: ["Choose subnets in at least two AZs for resilient capacity", "Set a launch template with AMI, instance type, IAM role, network, and user data", "Attach target groups and select health-check behavior", "Define scaling signals, warm-up, cooldown, and lifecycle behavior"],
    advancedConfiguration: ["Use capacity rebalancing for Spot interruptions", "Use instance refresh for controlled AMI rollout", "Set termination policies and lifecycle hooks", "Combine target tracking with scheduled scaling for predictable peaks"],
    connectionNotes: { "aws-alb": "Registers instances as healthy load-balancer targets.", "aws-ec2": "Represents the instances launched and managed by the group.", "aws-cloudwatch": "Consumes metrics and alarms to adjust capacity." },
  },
  "aws-ec2": {
    summary: ["EC2 provides configurable virtual machines when a workload needs operating-system, network, runtime, or storage control.", "The design decision includes the AMI, instance family, subnet, security groups, IAM role, user data, and replacement strategy."],
    features: ["Broad instance families and sizes", "AMI-based operating systems", "EBS and instance-store volumes", "Elastic IPs, ENIs, user data, and placement options", "On-Demand, Reserved, Savings Plans, and Spot purchasing"],
    useCases: ["Legacy applications", "Custom agents and runtimes", "Stateful or specialized compute", "Workloads not yet suitable for serverless or managed services"],
    limitations: ["You manage patching, hardening, capacity, and recovery", "An instance is AZ-scoped and can fail", "Instance store data is ephemeral", "Public IP exposure increases attack surface and operational responsibility"],
    configuration: ["Select AMI, instance type, tenancy, subnet, security groups, and IAM role", "Choose EBS type/size and encryption; distinguish boot from data volumes", "Use user data for repeatable bootstrap and tags for ownership", "Prefer private subnets plus load balancing for application fleets"],
    advancedConfiguration: ["Use launch templates and immutable replacement", "Model placement groups only for workloads that need their specific tradeoff", "Use Systems Manager instead of bastion access where possible", "Plan snapshots, AMI pipelines, quotas, and patch baselines"],
    connectionNotes: { "aws-alb": "Receives application traffic as a target group member.", "aws-rds": "Connects to a database over allowed security-group and route paths.", "aws-efs": "Mounts shared file storage through AZ-local mount targets.", "aws-sqs": "Consumes asynchronous work from a durable queue." },
  },
  "aws-lambda": {
    summary: ["Lambda runs functions in response to events without requiring server management. AWS handles infrastructure allocation while you configure code, runtime, memory, timeout, concurrency, and permissions.", "The key SAA design question is whether the workload is event-driven and short-lived enough for the execution model."],
    features: ["Managed runtimes and layers", "Event source mappings", "Reserved and provisioned concurrency", "Versions, aliases, and traffic shifting", "Integrated permissions and destinations"],
    useCases: ["API handlers", "S3 and EventBridge processing", "Queue consumers", "Scheduled automation", "Stream transformations"],
    limitations: ["Execution duration, payload, concurrency, and temporary storage are bounded", "Cold starts can affect latency", "Duplicate events and retries require idempotent code", "VPC attachment can add startup latency and requires valid egress paths"],
    configuration: ["Choose runtime, memory, timeout, architecture, and environment variables", "Assign an execution role with only required permissions", "Configure event source, batch size, retry, DLQ/destination, and concurrency", "Use structured logs and metrics for diagnosis"],
    advancedConfiguration: ["Use aliases and provisioned concurrency for controlled releases", "Use layers or container images with clear dependency ownership", "Model reserved concurrency to protect downstream systems", "Use VPC endpoints or NAT when private functions need AWS or internet access"],
    connectionNotes: { "aws-api-gateway": "Handles synchronous API requests.", "aws-s3": "Processes object-created events.", "aws-sqs": "Consumes buffered work with retry and batch semantics.", "aws-eventbridge": "Runs from scheduled or domain event patterns." },
  },
  "aws-rds": {
    summary: ["RDS is a managed relational database service for engines such as PostgreSQL and MySQL. AWS manages much of the infrastructure while you choose engine, class, storage, backups, and network placement.", "Multi-AZ improves availability through standby failover; read replicas are a separate read-scaling and replication decision."],
    features: ["Managed engines and automated backups", "Multi-AZ standby", "Read replicas", "Parameter and option groups", "Encryption, snapshots, and point-in-time restore"],
    useCases: ["Transactional relational applications", "Existing SQL workloads", "Applications requiring joins and ACID transactions", "Managed database tier behind private subnets"],
    limitations: ["Storage, connections, IOPS, and engine features vary by class and engine", "Multi-AZ standby is not a read replica", "Scaling and major version changes require planning", "The database remains stateful and needs backup/restore testing"],
    configuration: ["Create a DB subnet group covering at least two AZs", "Choose engine, class, storage type/size, encryption, credentials, and backup retention", "Allow only application security groups to reach the database port", "Enable deletion protection and monitoring for production"],
    advancedConfiguration: ["Tune parameter groups and connection pooling", "Plan replica promotion and cross-region disaster recovery", "Use Performance Insights and enhanced monitoring", "Test snapshots, point-in-time restore, failover, and schema migration paths"],
    connectionNotes: { "aws-ec2": "Provides private application-to-database connectivity.", "aws-auto-scaling": "Receives traffic from a scaled app tier, but scaling the DB is a separate concern.", "aws-cloudwatch": "Publishes database health, capacity, and performance signals." },
  },
  "aws-dynamodb": {
    summary: ["DynamoDB is a managed NoSQL key-value and document database designed for predictable low-latency access at scale.", "Tables should be modeled from access patterns first: partition key, optional sort key, indexes, capacity mode, and consistency choices determine the design."],
    features: ["On-demand and provisioned capacity", "Global secondary indexes", "Streams and TTL", "Point-in-time recovery and global tables", "Transactions and conditional writes"],
    useCases: ["Shopping carts and sessions", "Serverless application state", "High-scale key-value workloads", "Event-driven projections and metadata stores"],
    limitations: ["No joins or arbitrary relational queries", "Poor partition-key design creates hot partitions", "Item and transaction sizes are bounded", "Global tables introduce replication conflict and consistency considerations"],
    configuration: ["Define partition/sort keys from real access patterns", "Choose capacity mode and consistency intentionally", "Configure indexes, TTL, streams, encryption, backups, and alarms", "Use conditional writes and idempotency for safe retries"],
    advancedConfiguration: ["Use single-table design only when access patterns justify it", "Plan adaptive capacity and write sharding for hot keys", "Use global tables with conflict-resolution rules", "Use DAX only when the workload benefits from its cache semantics"],
    connectionNotes: { "aws-lambda": "Supports event-driven reads/writes and stream consumers.", "aws-api-gateway": "Provides a serverless API data store.", "aws-cloudwatch": "Exposes consumed capacity, throttles, and latency signals." },
  },
  "aws-sqs": {
    summary: ["SQS is a durable managed queue that decouples producers from consumers. It absorbs bursts and lets workers process work asynchronously.", "The queue is a reliability boundary: design visibility timeout, retries, idempotency, dead-letter handling, and ordering together."],
    features: ["Standard and FIFO queues", "Visibility timeout", "Long polling", "Dead-letter queues", "Server-side encryption and access policies"],
    useCases: ["Background jobs", "Buffering traffic spikes", "Service decoupling", "Reliable handoff between independent components"],
    limitations: ["Standard queues may deliver duplicates and out of order", "FIFO has throughput and grouping tradeoffs", "Messages need retention and visibility planning", "A queue does not perform business processing itself"],
    configuration: ["Choose standard or FIFO based on ordering and deduplication needs", "Set visibility timeout longer than normal processing time", "Configure max receives and a dead-letter queue", "Scale consumers from queue depth and age-of-oldest-message metrics"],
    advancedConfiguration: ["Use delay queues, message timers, and large-payload S3 patterns", "Use partial batch failure handling for Lambda consumers", "Protect producers and consumers with IAM conditions", "Design poison-message observability and replay procedures"],
    connectionNotes: { "aws-lambda": "Triggers functions from queued messages with managed polling.", "aws-ec2": "Provides durable work for worker instances.", "aws-sns": "Receives fan-out messages from a topic subscription.", "aws-cloudwatch": "Provides queue depth and age metrics for operations." },
  },
  "aws-cloudwatch": {
    summary: ["CloudWatch collects metrics, logs, traces, events, and alarms for AWS resources and applications.", "It is an observability and control-plane service: it can reveal failures and trigger actions, but it does not automatically make an architecture resilient."],
    features: ["Metrics and dashboards", "Logs and subscription filters", "Alarms and composite alarms", "EventBridge integration", "Container/application insights and anomaly detection"],
    useCases: ["Auto Scaling signals", "Operational dashboards", "Error and latency alerting", "Centralized log analysis", "Event-driven remediation"],
    limitations: ["Metrics have different resolution, retention, and cost characteristics", "Logs and custom metrics require deliberate retention and dimensions", "An alarm is only useful when thresholds and actions match the failure mode", "It is not a full distributed tracing replacement without the right instrumentation"],
    configuration: ["Select metrics, dimensions, periods, and thresholds based on user impact", "Set log groups, retention, structured fields, and subscription destinations", "Connect alarms to scaling, SNS, or remediation actions", "Create dashboards for golden signals and dependency health"],
    advancedConfiguration: ["Use metric math, anomaly detection, composite alarms, and cross-account observability", "Centralize logs while preserving account and region context", "Control cardinality and retention to manage cost", "Test alarm actions and incident runbooks instead of assuming they work"],
    connectionNotes: { "aws-auto-scaling": "Uses alarms and metrics to adjust capacity.", "aws-rds": "Monitors database health and capacity.", "aws-lambda": "Collects invocation, error, duration, and log signals." },
  },
  "aws-iam": {
    summary: ["IAM controls who or what can call AWS APIs and which resources those calls may access. It combines identities, roles, policies, resource policies, and conditions.", "IAM is global, but permissions still interact with regional services, organizations, SCPs, permission boundaries, and explicit denies."],
    features: ["Users, groups, roles, and policies", "Temporary credentials and STS", "Permission boundaries", "Resource-based policies", "MFA, conditions, and access analysis"],
    useCases: ["EC2 instance profiles", "Lambda execution roles", "Cross-account access", "Human federation and SSO", "Least-privilege service access"],
    limitations: ["An Allow is not sufficient when another policy layer explicitly denies", "IAM policy syntax and resource support vary by service", "Permissions can be hard to reason about without policy simulation and CloudTrail evidence", "Long-lived keys increase compromise impact"],
    configuration: ["Prefer roles and federation over long-lived access keys", "Start with least privilege and scope actions/resources/conditions", "Separate human, workload, deployment, and break-glass access", "Use MFA, credential rotation, tags, and CloudTrail for governance"],
    advancedConfiguration: ["Use Organizations SCPs as guardrails, not grants", "Model permission boundaries and session policies", "Use ABAC with principal/resource tags where it scales", "Review unused permissions and test cross-account trust policies"],
    connectionNotes: { "aws-ec2": "Uses an instance profile role for temporary credentials.", "aws-lambda": "Uses an execution role for service calls.", "aws-s3": "Combines identity and bucket policies to authorize object access." },
  },
  "aws-waf": {
    summary: ["AWS WAF filters HTTP and HTTPS requests at supported application entry points using managed and custom rules.", "It protects against application-layer patterns such as common exploits, abusive clients, and request-rate spikes; it is not a replacement for network controls."],
    features: ["Managed rule groups", "IP, geo, size, regex, and rate-based rules", "Web ACLs and rule priorities", "Count, block, allow, and CAPTCHA actions", "Sampled requests and logging"],
    useCases: ["Protect ALB, CloudFront, and API Gateway", "Block common web exploits", "Rate-limit abusive clients", "Create temporary incident response rules"],
    limitations: ["WAF does not inspect arbitrary non-HTTP traffic", "Rules need tuning to avoid false positives", "It does not replace security groups, IAM, or secure application code", "Managed rules and request inspection add cost and operational review"],
    configuration: ["Attach a Web ACL to the correct regional or global resource", "Order rules from specific exceptions to broad protections", "Start in count mode when tuning managed rules", "Enable logs and alarms for blocked requests and rule changes"],
    advancedConfiguration: ["Use scope-down statements and labels for layered policies", "Combine rate-based rules with trusted-source exceptions", "Use Firewall Manager for organization-wide policy deployment", "Review forwarded headers and client IP trust carefully"],
    connectionNotes: { "aws-cloudfront": "Filters global viewer requests at the edge.", "aws-alb": "Filters HTTP requests before they reach targets.", "aws-api-gateway": "Adds request-layer protection to managed APIs." },
  },
};
